git init to initialize repository

touch to create a new file
git add filename to add the file to the staging area

git commit to commit the files that are in the staging area

git remote add origin <git address> to add the remote repository

git push -u origin master to push the current branch into the remote repository
