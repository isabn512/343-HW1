We will use touch to add new files into the repository. To initialize a repository we can use git init. 

To add a file to the staging area use git add

To commit the files in the staging area use git commit
